logging:
  level:
    root: OFF
    org.springframework: OFF
    org.apache.kafka: OFF

server:
  port: 8082

spring:
  application.name: consumer
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9092